# ===========================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ===========================================
# IMPORTANT: Copy this file to .env.production and fill in real values
# NEVER commit .env.production to version control!

# Server Configuration
NODE_ENV=production
PORT=3000
API_PREFIX=/api

# Database Configuration (Use strong passwords!)
DATABASE_URL=postgresql://taxirelay_user:CHANGE_THIS_STRONG_PASSWORD@postgres:5432/taxirelay
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=taxirelay
DATABASE_USER=taxirelay_user
DATABASE_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
DATABASE_MAX_CONNECTIONS=50

# JWT Configuration (Generate secure random strings!)
# Use: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_USE_OPENSSL_RAND_BASE64_64
JWT_REFRESH_SECRET=CHANGE_THIS_USE_OPENSSL_RAND_BASE64_64_DIFFERENT
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration (Your production domains)
CORS_ORIGIN=https://taxirelay.com,https://www.taxirelay.com
WS_CORS_ORIGIN=https://taxirelay.com,https://www.taxirelay.com

# File Upload Configuration
MAX_FILE_SIZE=10485760
MAX_DOCUMENTS_PER_RIDE=5
MAX_CHAT_ATTACHMENT_SIZE=10485760
UPLOAD_PATH=/app/uploads

# Rate Limiting (Stricter for production)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=warn
LOG_FILE=/app/logs/app.log

# Security
BCRYPT_SALT_ROUNDS=12

# SSL/TLS (If terminating SSL at app level)
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem
